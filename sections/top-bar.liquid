{%- for block in section.blocks -%}
  {%- case block.type -%}
    {%- when 'announcement' -%}
    <div {{ block.shopify_attributes }}>
      <div class="w-full">
        <p class="text-center {{ blocks.settings.text_alignment }}">
          <span>{{ block.settings.text }}</span>
        </p>
      </div>
    </div>
  {%- endcase -%}
{%- endfor -%}

{% schema %}
  {
    "name": "Announcement",
    "max_blocks": 3,
    "class": "top-bar-section",
    "blocks": [
      {
        "type": "announcement",
        "name": "Announemcent",
        "settings": [
          {
            "type": "text",
            "id": "text",
            "default": "Welcome to our store",
            "label": "Edit Text"
          }, {
            "type": "select",
            "id": "text_alignment",
            "options": [
              {
                "value": "left",
                "label": "left"
              }, {
                "value": "center",
                "label": "center"
              }, {
                "value": "right",
                "label": "right"
              }
            ],
            "default": "center",
            "label": "Text Alignment"
          }
        ]
      }
    ]
  }
{% endschema %}